import { Box, Typography } from '@mui/material';

const Home = () => {
    return (
        <Box>
            <Typography variant="h1" color="white">
                Hi
            </Typography>
        </Box>
    );
};

export default Home;
