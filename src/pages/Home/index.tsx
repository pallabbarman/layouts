import { Box, Typography } from '@mui/material';

const Home = () => {
    return (
        <Box>
            <Typography variant="h1" color="error">
                Hi
            </Typography>
        </Box>
    );
};

export default Home;
